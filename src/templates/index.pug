doctype html
html(lang="en")
  head
    title= pageTitle
    link(rel="stylesheet", href="/custom_css/style.css")
  body
    center
      h1 Hello #{name} !
      p Hello world !
      p#foo
      p#token
      img(src="/images/logo.png")

    .day Day (initial)
    .day.light-scheme Day (changes in light scheme)
    .day.dark-scheme Day (changes in dark scheme)
    br
    .night Night (initial)
    .night.light-scheme Night (changes in light scheme)
    .night.dark-scheme Night (changes in dark scheme)

    //- google scripts
    script(src='https://www.gstatic.com/firebasejs/8.3.2/firebase-app.js')
    script(src='https://www.gstatic.com/firebasejs/8.3.2/firebase-messaging.js')
    script(src="/custom_firebase/firebase.js",type="module")